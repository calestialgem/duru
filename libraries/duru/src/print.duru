public void print(byte* string) {
  int result = print_string_to_stream(string, get_error_stream());
  if result < 0 {
    exit(result);
  }
}

struct stream;

stream* get_stream(uint index) = "__acrt_iob_func";

int print_string_to_stream(byte* string, stream* destination) = "fputs";

stream* get_error_stream() {
  return get_stream(2);
}

noreturn exit(int code) = "exit";
