{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch (Windows)",
      "type": "cppvsdbg",
      "request": "launch",
      "preLaunchTask": "${defaultBuildTask}",
      "program": "${workspaceFolder}/build/duru",
      "cwd": "${workspaceFolder}",
      "args": ["i"],
      "environment": [],
      "stopAtEntry": false,
      "console": "integratedTerminal"
    }
  ]
}
